# Flask-example
 Example of a minimal app using Flask

# Installation
Create a virtual environment
```shell
 py -3 -m venv .venv
 .venv\Scripts\Activate
```

Install Flask
```shell
pip install Flask
```

# Run the app

```shell
flask --app hello run
```

