# Flask-example
 Example of a minimal app using Flask

# Installation
Install neccessary library
```shell
pip install Flask
pip install Flask-SQLAlchemy
```

# Run the app

Create db
```shell
python models.py
```

Run app
```shell
python app.py
```
